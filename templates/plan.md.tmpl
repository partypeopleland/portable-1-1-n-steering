# Plan: ${objective}

- task_id: `${task_id}`
- revision: `${revision}`
- owner_authorization: ${owner_authorization}
- objective: ${objective}
- scope: ${scope}
- exclusions: ${exclusions}
- sources: ${sources}
- assumptions_and_dependencies: ${dependencies}
- ordered_steps: ${steps}
- risk_and_authority: ${risk_and_authority}
- validation: ${validation}
- rollback_and_safe_stop: ${rollback}
- deliverables: ${deliverables}
- acceptance: ${acceptance}

## Execution notes

The main task window does not perform substantive mutation until an independent
reviewer returns `LGTM` for this plan's exact SHA-256. Actual, planned, historical,
and runtime-unknown claims remain separate.
